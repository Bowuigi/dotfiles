# vim: ft=sh
path = (/bin /sbin /usr/bin /usr/sbin /usr/local/sbin /usr/local/bin $HOME/bin)
EDITOR = nvim
PAGER = less
WWW_HOME = searx.garudalinux.org
BROWSER = links -g
# ELinks for TUI, Links2 for Framebuffer

red = `{color red}
green = `{color green}
blue = `{color blue}
cyan = `{color cyan}
normal = `{color normal}
bold = `{color bold}

fn m { $EDITOR $* }
fn p { $PAGER $* }
fn S { $BROWSER $* }

fn gs { git status $* }
fn ga { git add $* }
fn gc { git commit $* }
fn gp { git push $* }
fn gP { git pull $* }

fn config { m ~/.esrc && . ~/.esrc }

fn l {
	ls '--color=auto' -SAhs $*
}

fn cleanpwd {
	pwd | sed 's#'^$HOME^'#~#gi'
}

fn spkg {
	yay '--color=always' -Ss $* | $PAGER -R
}

fn ñ { clear }

fn %prompt {
	prompt = ($green^$bold^[$bqstatus]^$normal^' -> '^$blue^`{cleanpwd}^$normal^\n^'> '^$bold '> ')
}
